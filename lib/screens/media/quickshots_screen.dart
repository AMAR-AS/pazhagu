
// lib/screens/media/quickshots_screen.dart

import 'package:flutter/material.dart';

class QuickshotsScreen extends StatelessWidget {
  const QuickshotsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Quickshots will be displayed here.'),
      ),
    );
  }
}
