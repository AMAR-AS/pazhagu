
// lib/styles/neumorphism/neumorphic_ui.dart

import 'package:flutter/material.dart';

class NeumorphicUI extends StatelessWidget {
  final Widget child;

  const NeumorphicUI({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // Placeholder for neumorphic UI style
      child: child,
    );
  }
}
