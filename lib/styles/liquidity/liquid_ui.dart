
// lib/styles/liquidity/liquid_ui.dart

import 'package:flutter/material.dart';

class LiquidUI extends StatelessWidget {
  final Widget child;

  const LiquidUI({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // Placeholder for liquid UI style
      child: child,
    );
  }
}
